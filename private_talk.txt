#ifndef UIIU_H
#define UIIU_H
#include <QWidget>
#include <QGridLayout>
#include <QHBoxLayout>
#include <QVBoxLayout>
#include <QPushButton>
#include <QTextEdit>
#include <QLineEdit>
#include <QLabel>
#include <QListWidget>
#include <QString>
class ooi:public QWidget
{
    Q_OBJECT
public:
    ooi();
    ~ooi();

    void on_button_send_private();
    void on_button_clear();

private:
   QPushButton * m_button_send_private  ,*m_button_clear;
   QTextEdit   * m_textEdt_private,*m_textEdt_send;
   QLabel      * m_label_msg;
   QGridLayout * m_gridlay;
};

ooi::ooi()
{
    m_button_send_private = new QPushButton(QString::fromLocal8Bit("·¢ËÍ"),this);
    m_button_clear        = new QPushButton(QString::fromLocal8Bit("Çå¿Õ"),this);

    m_textEdt_private     = new QTextEdit(this);
    m_textEdt_send        = new QTextEdit(this);

    m_label_msg           = new QLabel(this);

    m_gridlay             = new QGridLayout(this);

    m_gridlay->addWidget(m_textEdt_private,0,0,1,5);
    m_gridlay->addWidget(m_textEdt_send,1,0,1,5);
    m_gridlay->addWidget(m_button_clear,2,0,1,1);
    m_gridlay->addWidget(m_button_send_private,2,1,1,1);
    m_gridlay->addWidget(m_label_msg,2,2,1,1);

    m_gridlay->setRowStretch(0,3);
    m_gridlay->setRowStretch(1,1);
    this->resize(700,450);

    QObject::connect(m_button_clear,&QPushButton::clicked,&ooi::on_button_clear);
    QObject::connect(m_button_send_private,&QPushButton::clicked,&ooi::on_button_send_private);

}
#endif // UIIU_H

void ooi::on_button_clear()
{

}

void ooi::on_button_send_private()
{

}

ooi::~ooi()
{

}
